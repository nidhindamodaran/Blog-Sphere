<%= form_for ([@article, @article.comments.build]) do |f| %>
<table >
  <tr>
    <td><%= f.text_area :body, class: "form-control", :placeholder => "Add comment" %></td>
    <td><%= f.submit :submit.capitalize, :class => "btn btn-primary", :value => "Post" %></td>
  </tr>

</table>
<% end %>
